// src/data/components/utils.js
export const crearProducto = (codigo, nombre, imagenChica, imagenGrande, descripcion) => {
  return {
    codigo,
    nombre,
    imagenChica,
    imagenGrande,
    descripcion,
  };
};